//! 多 Agent 进程管理器 — stub
//!
//! 完整实现在 Task 2 中补充。

/// 多 agent 进程管理器
pub struct AgentManager;

impl AgentManager {
    /// 创建空的管理器
    pub fn new() -> Self {
        Self
    }
}

impl Default for AgentManager {
    fn default() -> Self {
        Self::new()
    }
}
